[project]
name = "pdx-mod-studio"
version = "0.1.0"
description = "Paradox Game Modding Studio"
authors = [
    { name = "User", email = "user@example.com" }
]
dependencies = [
    "PySide6",
]
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
