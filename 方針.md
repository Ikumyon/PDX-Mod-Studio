## 基本方針

```text
本体 = アプリとして必ず必要な共通機能
プラグイン = ゲーム別・MOD別・形式別の知識
```

もっと簡単に言うと、

```text
本体は「箱」
プラグインは「中身」
```

です。

また、テキスト（生コード）は前面に出さず、エラーの該当項目を開く場合であっても、該当する要素のカスタムタブで開くようにします。

---

# 本体にするもの

## 1. アプリの起動・終了

これは当然本体。

```text
・起動
・終了
・ウィンドウ表示
・設定読み込み
・最近開いたプロジェクト
```

---

## 2. プロジェクト管理

「MODプロジェクトを開く」という共通機能は本体。

```text
・フォルダを開く
・プロジェクト名
・プロジェクトパス
・保存状態
・最近開いたプロジェクト
・プロジェクト設定
```

ただし、
**そのフォルダが HOI4 MOD か CK3 MOD かを深く理解する部分**はプラグイン。

本体はせいぜい、

```text
このフォルダをプロジェクトとして開いた
```

まで。

---

## 3. プラグイン管理

これは本体。

```text
・プラグインの読み込み
・有効/無効
・対応ゲームの判定
・プラグイン一覧
・プラグイン設定
```

本体はプラグインに対して、

```text
このプロジェクトを扱える？
扱えるなら画面や機能を登録して
```

と聞く。

---

## 4. タブ・画面管理

これも本体。

```text
・中央タブ
・タブを開く
・タブを閉じる
・タブの順番変更
・未保存表示
・分割表示するならその管理
```

ただし、タブの中身はプラグイン。

```text
本体:
  タブを置く場所

プラグイン:
  Decision編集画面
  Event一覧
  Focus編集フォーム
```

---

## 5. 共通UI

ゲームに関係ないUIは本体。

```text
・メインウィンドウ
・メニュー
・ツールバー
・ステータスバー
・左サイドバー
・右サイドバー
・通知
・設定画面
・テーマ
・ショートカット
```

ただし、サイドバーの中に表示する **HOI4固有メニュー** はプラグイン。

---

## 6. ホーム画面

これは本体。

```text
・最近開いたプロジェクト
・新規プロジェクト
・プラグイン一覧
・設定
・ヘルプ
```

これは MOD 固有ではないので本体。

---

## 7. ダッシュボードの枠

ダッシュボードを表示する仕組みは本体。

```text
・ダッシュボードを開く
・カードを並べる
・更新ボタン
・通知表示
・クリックイベントを受ける
```

ただし、**MODダッシュボードの中身**はプラグイン。

```text
本体:
  ダッシュボードを表示する場所

プラグイン:
  HOI4 MODダッシュボードの中身
```

---

## 8. 検索の枠

検索UIと検索結果表示は本体。

```text
・検索バー
・検索結果一覧
・検索フィルター
・結果をクリックして開く
・ショートカット
```

ただし、何を検索対象にするかはプラグイン。

```text
本体:
  検索画面

プラグイン:
  decision検索
  event検索
  focus検索
  localisation検索
```

---

## 9. 共通ファイル操作

ファイルを読む・保存する共通機能は本体。

```text
・ファイル読み込み
・ファイル保存
・文字コード
・変更検知
・バックアップ
・自動保存
```

ただし、ファイルの意味を理解するのはプラグイン。

```text
本体:
  ファイルを読む

プラグイン:
  これは decision ファイルである
  この中に decision ID がある
```

---

## 10. 共通エラー・診断表示

エラー一覧を表示する場所は本体。

```text
・エラー一覧
・警告一覧
・クリックで該当項目を開く（テキスト（生コード）は前面に出さず、カスタムタブで開く）
・重大度表示
```

ただし、エラーを見つけるのはプラグイン。

```text
本体:
  エラー表示欄

プラグイン:
  localisation が存在しない
  GFX参照が切れている
  focus tree の位置が重複している
```

---

# プラグインにするもの

## 1. ゲーム別知識

これは全部プラグイン。

```text
・HOI4
・CK3
・EU4
・Victoria 3
・Stellaris
```

本体に `HOI4` という知識を入れない。

---

## 2. MOD構造の理解

例えば HOI4 なら、

```text
common/
events/
history/
localisation/
gfx/
interface/
map/
```

こういう構造を理解するのは HOI4 プラグイン。

本体はただのフォルダとして見る。

---

## 3. リソース種別

これもプラグイン。

```text
HOI4プラグイン
├─ decision
├─ event
├─ national_focus
├─ idea
├─ technology
├─ country
├─ state
├─ localisation
└─ gfx
```

本体は `decision` が何か知らなくていい。

---

## 4. 入力フォーム

入力フォームはプラグイン側。

```text
・Decision編集フォーム
・Event編集フォーム
・National Focus編集フォーム
・Idea編集フォーム
・Country編集フォーム
・State編集フォーム
```

理由は、フォーム項目がゲームごとに違うから。

---

## 5. MODダッシュボードの中身

これはプラグイン側。

HOI4なら例えば、

```text
・MOD名
・対応バージョン
・decision数
・event数
・focus数
・未翻訳数
・エラー数
・画像参照切れ
・最近編集したHOI4要素
```

CK3なら別になる。

だから本体に置かない。

---

## 6. 解析器

これはプラグイン側。

```text
・Paradox構文解析
・CWT解釈
・decision解析
・event解析
・localisation解析
・gfx参照解析
```

ただし、**完全に共通化できる低レベル構文パーサー**だけは本体寄りでもいい。

でも設計リセットなら、最初はこう考えると安全。

```text
ゲーム/MODに意味がある解析 = プラグイン
ただのテキスト処理 = 本体
```

---

## 7. バリデーション

これもプラグイン。

```text
・ID重複
・参照切れ
・localisation不足
・必須項目不足
・構文エラー
・ゲーム仕様違反
```

本体はそれを一覧表示するだけ。

---

## 8. 検索対象

検索画面は本体でも、検索対象はプラグイン。

```text
HOI4プラグインが登録する検索対象
├─ Decision
├─ Event
├─ Focus
├─ Idea
├─ Localisation
├─ GFX
└─ Error
```

本体は、

```text
検索語を各プラグインに渡す
結果を並べる
```

だけ。

---

## 9. リソース一覧画面

これもプラグイン。

```text
・Decision一覧
・Event一覧
・Focus一覧
・Localisation一覧
・画像一覧
```

本体は一覧画面を置く場所だけ提供。

---

## 10. 保存形式への変換

フォームで編集した内容を実ファイルへ戻す処理はプラグイン。

```text
フォーム入力
↓
HOI4 decision形式に変換
↓
common/decisions/*.txt に保存
```

これは HOI4 固有なので本体に入れない。

---

# 境界線まとめ

```text
本体に入れる
├─ アプリ起動
├─ ウィンドウ
├─ タブ
├─ サイドバー
├─ プロジェクトを開く
├─ プラグイン読み込み
├─ 検索UI
├─ 診断表示UI
├─ ダッシュボード表示枠
├─ 共通設定
├─ 共通ファイル操作
└─ ホーム画面

プラグインに入れる
├─ HOI4/CK3などゲーム知識
├─ MODフォルダ構造
├─ decision/event/focusなどのリソース定義
├─ ダッシュボードの中身
├─ 入力フォーム
├─ リソース一覧
├─ 解析
├─ バリデーション
├─ localisation処理
├─ 参照関係解析
├─ 検索対象
└─ 保存時の変換
```

---

# 判断基準

迷ったらこれで判断するといいです。

## 本体に置くもの

```text
HOI4以外でも使う
CK3でもEU4でも使う
MOD以外のプロジェクトでも使えそう
ゲーム名を知らなくていい
```

例：

```text
タブ管理
検索欄
プロジェクトを開く
設定
通知
```

---

## プラグインに置くもの

```text
HOI4だから必要
CK3だと違う
ファイルの意味を知る必要がある
ゲームの仕様を知る必要がある
```

例：

```text
national_focus
decision
event
localisation
common/decisions
focus tree
```

---

# ダッシュボードについての結論

```text
本体:
  ダッシュボードを表示する仕組み

プラグイン:
  MODダッシュボードの内容
```

つまり、

```text
ホームダッシュボード = 本体
HOI4 MODダッシュボード = HOI4プラグイン
CK3 MODダッシュボード = CK3プラグイン
```

が一番きれいです。

設計リセットするなら、最初からこの分け方でいいです。

---

# 実装の順序

本体とプラグインの分離、およびカスタムタブの連携を進めるにあたり、以下の順序で実装を行います。

## フェーズ 1: プラグイン・マネージャーと grammar 資産ロードの厳格化
- **やること**: 
  - `core/syntax_engine`（`GrammarAssetLoader` 等）において、プラグイン側から読み込むべき cwt / toml などの外部ファイル（grammar 資産ファイル）が無い場合に、フォールバックやコード側での補完をせず、エラーとして扱うように修正。
  - `syntax.toml`（構文定義）は廃止せず残し、必ずプラグイン側の実ファイルからロードするように修正。
  - 翻訳キーが未翻訳の際、デフォルトの人間向け文言を使用せず、翻訳キー文字列そのものを表示（fallback）するように修正。
  - 本体コードに grammar 資産の既定値をハードコードせず、必ずプラグイン側の実ファイルを読み込むように修正。

## フェーズ 2: カスタムタブおよびダッシュボード表示枠（本体）の整備
- **やること**:
  - プラグインが本体にカスタムタブ（エディタ）やダッシュボードを登録する共通APIの整備。
  - ファイルオープン時、標準テキストエディタではなく、登録されたカスタムタブを最優先で表示する構造への変更。
  - 本体はダッシュボードを表示する枠組み（コンテナ）のみを提供し、中身はプラグインから動的に取得するように変更。

## フェーズ 3: 診断表示（本体）とエラー発生（プラグイン）およびカスタムタブ連携の実装
- **やること**:
  - バリデーション（エラー検出）はプラグイン側で実行し、本体はそれを一覧表示するだけにする。
  - エラー一覧の項目をクリックした際、生テキストエディタではなく、該当要素のカスタムタブを開いてジャンプする連携処理の実装。

## フェーズ 4: プラグイン側（HOI4等）の具体的中身の実装
- **やること**:
  - HOI4用の `plugin_manifest.json` と各種 grammar 資産の整備。
  - HOI4ダッシュボード（MOD名、対応バージョン、要素数、エラー数など）を返す処理を実装。
  - DecisionやEvent用のカスタム編集タブ（入力フォーム）、およびParadox構文への相互変換ロジックを実装.
  - ID重複や参照切れなどのバリデーションルールを実装。

## フェーズ 5: 共通検索（本体）と検索対象（プラグイン）の統合・結合テスト
- **やること**:
  - 本体の検索UIからプラグイン側の検索処理を呼び出して結果を並べる。
  - テスト用MODプロジェクトを読み込み、ダッシュボード、カスタムタブ、エラー、ジャンプ、保存変換の一連の流れを結合テスト。

